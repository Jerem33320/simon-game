/**
 * Get a random integer betwenn 0 (included) and limit (excluded)
 * @param {number} limit
 * @returns {number} a random integer
 */
function randomInt(limit) {
  return Math.floor(Math.random() * limit);
}
